.env
.venv/
__pycache__/
*.pyc
*.pyo
.idea/
*.egg-info/

# Claude Code: all assistant-related files and session data
.claude/
.claude.json
.mcp.json
CLAUDE.md
CLAUDE.local.md
memory/
MEMORY.md
*.jsonl.bak

# External source repo — cloned separately (see README §1), not vendored here
winmerge/

# MLflow runs (generated data, not source)
mlruns/

# SQLite metrics DB (regenerated by runner)
reports/metrics.db

# Cached dense-retrieval embeddings (regenerated from corpus on first run)
reports/.emb_cache/
